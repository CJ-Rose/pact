import React, { FC } from "react"

const Wallet: FC = () => {
    return (
        <div>
            <button className="bg-brand-orange rounded-lg px-4 py-2 text-brand-purple">connect wallet</button>
        </div>
    )
}

export default Wallet